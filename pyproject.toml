[project]
name = "tweets"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "fyzan", email = "finamdar204@gmail.com" }
]
requires-python = ">=3.12,<3.14"
dependencies = [
    "jupyter",
    "matplotlib",
    "pandas",
    "pyarrow",
    "scikit-learn",
    "torch",
    "sentence-transformers",
]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[tool.uv.sources]
torch = { index = "pytorch-cpu"}

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.7",
]
